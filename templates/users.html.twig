{% extends 'base.html.twig' %}

{% block body %}
	<h1 class="text-center py-5">Liste des utilisateurs</h1>
	<table class="table">
		<thead>
			<tr>
				<th scope="col">Prénom</th>
				<th scope="col">Nom</th>
				<th scope="col">Email</th>
				<th scope="col">Profil</th>
			</tr>
		</thead>
		<tbody>

			{% for user in users %}
				<tr>
					
					<td>{{user.prenom}}</td>
					<td>{{user.nom}}</td>
					<td>{{user.email}}</td>
                   	<td>{{user.roles|join}}</td>
				</tr>
			{% endfor %}
		</tbody>
	</table>

{% endblock %}
